{% extends "SiplecCdBundle::layout.html.twig" %}

{% block title entity|capitalize %}

{% block content -%}
    <h1>Utilisateur {{ entity }}</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entity.nom }} {{ entity.prenom }}</td>
            </tr>
            <tr>
                <th>En cours</th>
                <td>{{ entity.enabled }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('utilisateur') }}">
            Retour à la liste
        </a>
    </li>
</ul>
{% endblock %}
